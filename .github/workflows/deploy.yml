name: Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable Corepack
        run: corepack enable

      - name: Install pnpm
        run: corepack prepare pnpm@9 --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: pnpm --filter=frontend build

      - name: Build backend
        run: pnpm --filter=api build

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.CPANEL_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H 69.62.85.170 >> ~/.ssh/known_hosts

      - name: Test SSH connection
        run: |
          ssh -o StrictHostKeyChecking=no salesnest@69.62.85.170 "echo SSH-OK"

      - name: Deploy frontend
        run: |
          rsync -avz --delete \
            frontend/dist/ \
            salesnest@69.62.85.170:/home/salesnest/public_html/

      - name: Deploy backend
        run: |
          rsync -avz --delete \
            backend/ \
            salesnest@69.62.85.170:/home/salesnest/public_html/api.crm.nahatalawns.com/

      - name: Install backend dependencies
        run: |
          ssh salesnest@69.62.85.170 \
          "cd /home/salesnest/public_html/api.crm.nahatalawns.com && corepack enable && corepack prepare pnpm@9 --activate && pnpm install --prod"

      - name: Restart backend
        run: |
          ssh salesnest@69.62.85.170 \
          "cd /home/salesnest/public_html/api.crm.nahatalawns.com && pkill -f 'node.*dist' || true; nohup node dist/index.js > app.log 2>&1 &"
